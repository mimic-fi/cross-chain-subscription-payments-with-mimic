{
  "name": "cross-chain-subscription-payments-with-mimic",
  "private": true,
  "version": "1.0.0",
  "description": "Cross chain subscription payments with Mimic",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter cross-chain-subscription-payments-with-mimic-ui dev",
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/mocha",
      "**/mocha/**"
    ]
  },
  "dependencies": {
    "@mimicprotocol/sdk": "0.0.1-rc.33",
    "lucide-react": "^0.454.0",
    "next": "^16.1.6",
    "react": "19.2.0",
    "wagmi": "^2.19.5"
  }
}
