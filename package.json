{
  "name": "cross-chain-subscription-payments-with-mimic",
  "version": "1.0.0",
  "description": "Cross chain subscription payments with Mimic",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/mocha",
      "**/mocha/**"
    ]
  }
}
